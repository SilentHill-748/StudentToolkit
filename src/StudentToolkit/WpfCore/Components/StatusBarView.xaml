<UserControl x:Class="StudentToolkit.WpfCore.Components.StatusBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ViewModel="clr-namespace:StudentToolkit.MVVM.ViewModels.Components"
             d:DataContext="{d:DesignInstance Type=ViewModel:StatusBarViewModel}"
             mc:Ignorable="d"
             SnapsToDevicePixels="True">

    <Border BorderBrush="Black" BorderThickness="0 1 0 0">
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="{Binding StringFormat={}Сегодня: {0:yyyy-MM-dd HH:mm:ss}, Path=CurrentDate}"
                       Margin="10 0 0 0"/>
            
            <TextBlock Text="{Binding StringFormat={}Группа: {0}, Path=GroupCode}"
                       Margin="10 0 0 0"/>
        </StackPanel>
    </Border>
</UserControl>
