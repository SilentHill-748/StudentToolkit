<Window x:Class="StudentToolkit.MVVM.Views.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:VM="clr-namespace:StudentToolkit.MVVM.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=VM:NavigationViewModel}"
        WindowStartupLocation="CenterScreen"
        Title="{Binding CurrentViewModel.WindowTitle}"
        Style="{StaticResource WindowStyle}">

    <ContentControl Content="{Binding CurrentViewModel}"/>
</Window>
