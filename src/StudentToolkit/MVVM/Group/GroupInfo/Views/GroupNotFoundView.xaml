<UserControl x:Class="StudentToolkit.MVVM.Group.GroupInfo.Views.GroupNotFoundView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ViewModel="clr-namespace:StudentToolkit.MVVM.Group.GroupInfo.ViewModels"
             d:DataContext="{d:DesignInstance Type=ViewModel:GroupNotFoundViewModel}"
             mc:Ignorable="d">

    <Grid>
        <StackPanel
            VerticalAlignment="Center"
            HorizontalAlignment="Center">

            <TextBlock
                Text="Похоже, Вы ещё не создавали группу..."
                Margin="0 0 0 25"/>

            <Button
                HorizontalAlignment="Center"
                Content="А давайте создадим её..."
                Command="{Binding NavigateToCreateGroupViewCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
