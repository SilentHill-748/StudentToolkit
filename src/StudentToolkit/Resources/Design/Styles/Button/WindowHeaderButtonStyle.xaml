<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="BaseButtonStyle.xaml"/>
        <ResourceDictionary Source="Templates/WindowHeaderButtonTemplate.xaml"/>
    </ResourceDictionary.MergedDictionaries>
    
    <Style
        x:Key="WindowHeaderButtonStyle"
        TargetType="{x:Type Button}"
        BasedOn="{StaticResource BaseButtonStyle}">

        <Setter Property="Width" Value="45"/>
        <Setter Property="Padding" Value="0 5"/>
        <Setter Property="Background" Value="{DynamicResource WindowHeaderBackgroundBrush}"/>
        <Setter Property="Template" Value="{StaticResource WindowHeaderButtonTemplate}"/>

        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource MouseSelectionBackgroundBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource MouseSelectionForegroundBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>